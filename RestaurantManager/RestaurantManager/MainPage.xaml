<Page
    x:Class="RestaurantManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Blue" >
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" Text="Resturant Manager" Margin="377,259,365,10" Style="{StaticResource TextBlockStyle}"/>
        <TextBlock Grid.ColumnSpan="2" Text="Internal Test Version" Margin="377,106,365,10" Grid.Row="1" Style="{StaticResource TextBlockStyle}"/>
        <Button Content="Go to Orders View" Grid.Column="0" Grid.Row="2" Margin="86,126,95,130" Click="Expedite_Click" Style="{StaticResource ButtonStyle}"/>
        <Button Content="Submit Order" Grid.Column="1" Grid.Row="2" Margin="128,127,127,130" Click="Order_Click" Style="{StaticResource ButtonStyle}"/>
    </Grid>
</Page>
